import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Schedule Calendar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
